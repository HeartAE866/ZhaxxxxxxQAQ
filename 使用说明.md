# ZhaxxxxxxQAQ 使用说明 / User Manual

轻量桌面工作记事录：无边框磨砂悬浮记事挂件，支持工作记录、待办事项、循环任务、提醒与下班倒计时，完全本地离线运行。
A lightweight desktop work journal: a frameless frosted floating widget with work logs, todos, recurring tasks, reminders, and an off-work countdown. Fully offline.

---

## English

### 1. Launch

| File | Purpose |
| ---- | ------- |
| `ZhaxxxxxxQAQ.vbs` | **Daily launch (recommended)**: double-click, no console window, quietly stays in the tray |
| `启动 ZhaxxxxxxQAQ.bat` | Debug launch: with a console window for output |

- Auto-start is written to `HKCU\...\Run` on first run (toggle in Settings → Reminders).
- To quit: right-click the tray icon → **Quit** (closing the main window only hides it).

### 2. Main Window (Desktop Floating Widget)

- **Frameless frosted floating window**: sits on the desktop layer, no taskbar entry, tray icon only.
- **Top bar**: 「⚡ Get to Work」button + search box + (optionally) a date/time clock. The clock is on by default and can be disabled in **Settings → Personalization → Show Clock**.
- **⚡ Get to Work** (top button / tray menu):
  - **Log past work**: pick any past date/time to backfill history;
  - **Start new work**: create a todo with a deadline;
  - **Add recurring task**: daily/weekly/monthly/quarterly/yearly.
- **Move**: drag the blank top area; the tray menu has **Lock Position**.
- **Resize**: hover any edge/corner, drag the double arrow; width is remembered.
- **Grouping**: records/todos are grouped by **creation time** into Year → Month → Tasks (collapsible; current year/month open by default). No scrollbar — the window height auto-fits the expanded groups.
- **Recurring tasks** live in their own 「🔁 Recurring Tasks」section; they stay *pending* (not struck through), showing 「⚠ Current due HH:MM」or the next reminder time.
- **Done**: work records are struck through on creation; todos via right-click → Mark Done; recurring tasks via "Complete Current".
- **Overdue**: overdue todos/recurring tasks turn red with ⚠.
- **📁 Folders**: when adding/editing an item, choose "auto-create by rule (default)" or "choose a folder manually". Clicking the folder icon opens the folder — bound folders open directly, unbound ones are auto-created: `Work Folder\2026\07\2026.7.31TaskName` (duplicates get a suffix); recurring tasks → `Work Folder\Recurring\TaskName`.
- **Double-click** an item to open the details panel (edit / done / tags / folder binding / rebind).
- **Drag to reorder**: hold the 「≡」handle on the left of a todo and drag; saved automatically.
- **Search**: the top search box does full-text search (including done items).
- **Compact mode**: tray menu switches to a mini bar showing the most urgent item; click to expand.

### 3. Tray Menu

Right-click the tray icon: Show/Hide main window, ⚡ Get to Work, Compact Mode, Lock Position, Always on Top, Click-Through, Settings, Quit.

### 4. Default Hotkeys (editable in Settings)

| Hotkey | Action |
| ------ | ------ |
| `Ctrl+Shift+Z+X` | Open Settings (from anywhere on the desktop) |
| `Ctrl+Shift+Z+P` | Toggle click-through (semi-transparent toast at top-right) |

Custom combos (up to 4 keys) can be assigned in Settings → Hotkeys for: quick record / quick todo / search / compact mode / show-hide.

### 5. Settings (tray → Settings)

1. **Personalization**: colors for every element (with on-screen eyedropper + hex input), font/size, background opacity, corner radius, clock on/off, dark/light presets, theme save/import/export; **Language (简体中文 / English)**.
2. **Folders**: base folder, multiple parallel generation rules (create/rename/delete), custom subfolders (with batch delete).
3. **Reminders**: default advance time, recurring/reminder toggles, off-work countdown, auto-start.
4. **Hotkeys**: view/modify/clear all hotkeys.
5. **Data**: search/filter all items, export JSON/CSV (saved to `导出\`), import with duplicate handling (skip or overwrite), guarded deletion (optional folder deletion), recurring history, tag manager.
6. **Logs**: view runtime/error logs (`logs\`), export.
7. **About**: version & author contact.

### 6. Directory Layout

```
ZhaxxxxxxQAQ\
├── ZhaxxxxxxQAQ.vbs         Silent launcher (auto-start target)
├── 启动 ZhaxxxxxxQAQ.bat     Debug launcher
├── 使用说明.md               This manual
├── config.json              All settings
├── app\                     Source code
├── venv\                    Bundled Python environment
├── resources\               Icons & author logo
├── data\data.json           All item data
├── logs\                    Runtime/error logs
├── 导出\                    Exported CSV/JSON/themes/logs
└── 工作文件夹\              Default work folder for bound items
```

- Uninstall: Quit from the tray, then delete this folder (the registry Run key is named `ZhaxxxxxxQAQ`).
- Migration: copy the whole folder to a new location and run the vbs — nothing is lost.

### 7. Reminders

- Todos: popup + tray balloon + main-window highlight when "deadline − advance time" is reached.
- Recurring tasks: reminded per period & time.
- Reminder popup options: ✔ Done / Snooze 5·30·60 min / custom minutes / Close.

---

## 简体中文

### 一、启动方式

| 文件 | 用途 |
| ---- | ---- |
| `ZhaxxxxxxQAQ.vbs` | **日常启动（推荐）**：双击即可，无黑窗口，安静驻留托盘 |
| `启动 ZhaxxxxxxQAQ.bat` | 调试启动：带控制台窗口，可查看输出 |

- 首次运行后已自动写入注册表 `HKCU\...\Run` 实现**开机自启**（可在 设置 → 提醒 中关闭）。
- 退出方式：右键系统托盘图标 → **退出程序**（直接关主窗口只是隐藏）。

### 二、主界面（桌面悬浮窗）

- **无边框磨砂悬浮窗**：默认贴在桌面层，不显示在任务栏，只在系统托盘有图标。
- **顶部栏**：「⚡ 来活了」按钮 + 搜索框 +（可选）日期时间时钟。时钟默认显示，可在 **设置 → 个性化** 取消勾选「显示时钟」关闭。
- **⚡ 来活了**（顶部按钮，托盘菜单同名）：
  - **登记以往工作**：可自由选择过去的日期时间，补录历史工作；
  - **开始新工作**：新建带截止时间的待办事项；
  - **添加循环任务**：新建循环任务（每天/每周/每月/每季/每年）。
- **拖动**：按住顶部空白处拖动窗口；托盘菜单可「锁定位置」。
- **调整大小**：鼠标移到窗口任意边缘/角落，出现双向箭头后拖拽即可自由缩放，宽度自动保存。
- **收纳**：工作记录/待办按**创建时间**收纳为「年 → 月 → 当月任务」两层结构，单击可折叠/展开，默认展开当年当月；不再使用滚动条，窗口高度随内容**平铺**自适应。
- **循环任务**单独一个「🔁 循环任务」独立区，不参与年月收纳；总是保持**未完成**状态（不划线），行内显示「⚠ 当期未完成 HH:MM」或下一次提醒时间。
- **完成划除**：工作记录添加即划线；待办右键 → 标记完成；循环任务可"完成当期"。
- **逾期**：过期未完成的待办/循环任务标题变红并带 ⚠ 提示。
- **📁 文件夹**：添加/编辑事项时可选择「自动创建（默认）」或「手动选择文件夹」绑定；点击事项后的文件夹图标——已绑定则直接打开；未绑定则按规则自动创建：`工作文件夹\2026年\7月\2026.7.31事项名`（重名自动追加序号）；循环任务 → `工作文件夹\循环任务\任务名`。
- **双击事项**打开详情面板（编辑/完成/标签/文件夹绑定/改绑）。
- **拖拽排序**：按住待办左侧「≡」手柄上下拖动即可，自动保存。
- **搜索**：主界面顶部搜索框按关键词全文搜索（含已完成事项）。
- **紧凑模式**：托盘菜单切换为迷你悬浮条，仅显示最紧急事项，点击展开。

### 三、系统托盘菜单

右键托盘图标：显示/隐藏主界面、⚡ 来活了、紧凑模式、锁定位置、窗口置顶、鼠标穿透、设置、退出程序。

### 四、默认快捷键（可在设置中修改/扩充）

| 快捷键 | 功能 |
| ------ | ---- |
| `Ctrl+Shift+Z+X` | 打开设置窗口（桌面任意位置可呼出） |
| `Ctrl+Shift+Z+P` | 切换鼠标穿透（屏幕右上角有半透明提示） |

设置 → 快捷键 中还可为"快速记录 / 快速待办 / 搜索 / 紧凑模式 / 显示隐藏"绑定自定义组合键（支持四键组合）。

### 五、设置（托盘 → 设置）

1. **个性化**：界面各元素颜色（含屏幕吸管取色、十六进制输入）、系统字体/字号、背景不透明度、圆角大小、顶部时钟显示开关、暗色/亮色预设主题，主题保存/导入/导出；**语言（简体中文 / English）**。
2. **文件夹**：默认工作文件夹父目录；**多条平行生成规则**（新建/重命名/删除）；快捷自定义子文件夹（支持批量删除）。
3. **提醒**：待办默认提前量、循环/提醒开关、**下班倒计时**、开机自启开关。
4. **快捷键**：查看/修改/清除所有快捷键。
5. **数据管理**：搜索/筛选全部事项、导出 JSON/CSV（保存在 `导出\`）、导入合并（重复项可跳过或覆盖）、**防误删删除**（逐条确认，可选同时删除绑定文件夹）、循环任务完成历史、标签管理。
6. **日志**：查看运行日志与错误日志（`logs\`），支持导出。
7. **关于**：版本与作者联系方式。

### 六、目录结构

```
ZhaxxxxxxQAQ\
├── ZhaxxxxxxQAQ.vbs        静默启动器（开机自启指向它）
├── 启动 ZhaxxxxxxQAQ.bat    调试用启动器
├── 使用说明.md              使用说明
├── config.json             全部设置（主题/快捷键/窗口位置等）
├── app\                    程序源码（main.py 等）
├── venv\                   内置 Python 运行环境（无需系统安装依赖）
├── resources\              图标与作者 logo
├── data\data.json          全部事项数据
├── logs\                   运行/错误日志
├── 导出\                   导出的 CSV/JSON/主题/日志
└── 工作文件夹\             事项绑定的默认工作目录
```

- 卸载：托盘 → 退出程序后，直接删除本文件夹即可（注册表 Run 项名为 ZhaxxxxxxQAQ，可自行清理）。
- 迁移：整个文件夹复制到新位置后运行 vbs 即可，数据不丢失。

### 七、提醒说明

- 待办：到达"截止时间 − 提前量"时弹窗 + 托盘气泡 + 主界面高亮。
- 循环任务：按周期与设定时间提醒当期实例。
- 提醒弹窗支持：✔完成 / 稍后 5 分钟、30 分钟、1 小时 / 自定义分钟数 / 关闭。
