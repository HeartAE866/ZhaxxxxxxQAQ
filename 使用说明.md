# ZhaxxxxxxQAQ —— 轻量桌面工作记事录

一个贴在桌面上的无边框磨砂悬浮记事挂件，支持工作记录、待办事项、循环任务，
完全本地离线运行，所有数据保存在本目录内。

## 一、启动方式

| 文件 | 用途 |
| ---- | ---- |
| `ZhaxxxxxxQAQ.vbs` | **日常启动（推荐）**：双击即可，无黑窗口，安静驻留托盘 |
| `启动 ZhaxxxxxxQAQ.bat` | 调试启动：带控制台窗口，可查看输出 |

- 首次运行后已自动写入注册表 `HKCU\...\Run` 实现**开机自启**（可在 设置 → 提醒 中关闭）。
- 退出方式：右键系统托盘图标 → **退出程序**（直接关主窗口只是隐藏）。

## 二、主界面（桌面悬浮窗）

- **无边框磨砂悬浮窗**：默认贴在桌面层，不显示在任务栏，只在系统托盘有图标。
- **顶部栏**：「⚡ 来活了」按钮 + 日期时间时钟 + 搜索框。时钟默认显示，
  可在 **设置 → 个性化** 取消勾选「显示时钟」关闭；
  下方为任务列表区，背景与主窗完全一致。
- **⚡ 来活了**（顶部按钮，托盘菜单同名）：
  - **登记以往工作**：可自由选择过去的日期时间，补录历史工作；
  - **添加待办**：新建带截止时间的待办事项；
  - **添加循环任务**：新建循环任务（每天/每周/每月/每季/每年）。
- **拖动**：按住顶部空白处拖动窗口；托盘菜单可「锁定位置」。
- **调整大小**：鼠标移到窗口任意边缘/角落，出现双向箭头后拖拽即可自由缩放，宽度自动保存。
- **收纳**：工作记录/待办按**创建时间**收纳为「年 → 月 → 当月任务」两层结构，
  单击「2026年」/「8月」标题可折叠/展开，默认展开当年当月；
  不再使用滚动条，任务随内容**平铺**展开，窗口高度自动适配当前展开的分组。
- **循环任务**单独一个「🔁 循环任务」独立区，不参与年月收纳；循环任务总是保持**未完成**状态
  （不划线），行内显示「⚠ 当期未完成 HH:MM」或下一次提醒时间与周期说明。
- **完成划除**：工作记录添加即划线；待办右键 → 标记完成；循环任务可“完成当期”。
- **逾期**：过期未完成的待办/循环任务标题变红并带 ⚠ 提示。
- **📁 文件夹**：添加/编辑事项时可选择「自动创建（默认）」或「手动选择文件夹」绑定；
  点击事项后的文件夹图标——已绑定则直接打开；未绑定则按规则自动创建：
  - 记录/待办：`工作文件夹\2026年\7月\2026.7.31事项名`（重名自动追加序号）
  - 循环任务：`工作文件夹\循环任务\任务名`（历史资料共用）
- **双击事项**打开详情面板（编辑/完成/标签/文件夹绑定/改绑）。
- **拖拽排序**：按住待办左侧「≡」手柄上下拖动即可，自动保存。
- **搜索**：主界面顶部搜索框按关键词全文搜索（含已完成事项）。
- **标签筛选**：可在 设置 → 数据管理 中按标签筛选和管理。
- **紧凑模式**：托盘菜单切换为迷你悬浮条，仅显示最紧急事项，点击展开。
- 主界面保持极简，无 logo 等冗余元素（作者 logo 见 设置 → 关于）。

## 三、系统托盘菜单

右键托盘图标：显示/隐藏主界面、⚡ 来活了、紧凑模式、锁定位置、窗口置顶、
鼠标穿透、设置、退出程序。

## 四、默认快捷键（可在设置中修改/扩充）

| 快捷键 | 功能 |
| ------ | ---- |
| `Ctrl+Shift+Z+X` | 打开设置窗口（桌面任意位置可呼出） |
| `Ctrl+Shift+Z+P` | 切换鼠标穿透（屏幕右上角有半透明提示） |

设置 → 快捷键 中还可为“快速记录 / 快速待办 / 搜索 / 紧凑模式 / 显示隐藏”
绑定自定义组合键（支持四键组合）。

## 五、设置（托盘 → 设置）

1. **个性化**：界面各元素颜色（含屏幕吸管取色、十六进制输入）、系统字体/字号、
   背景不透明度、磨砂强度、圆角大小、顶部时钟显示开关、暗色/亮色预设主题，
   支持主题保存/导入/导出。
2. **文件夹**：默认工作文件夹父目录。
3. **提醒**：待办默认提前量、循环提醒开关、开机自启开关。
4. **快捷键**：查看/修改/清除所有快捷键。
5. **数据管理**：搜索/筛选全部事项、导出 JSON/CSV（保存在 `导出\`）、导入合并
   （重复项可跳过或覆盖）、**防误删删除**（逐条确认，可选同时删除绑定文件夹）、
   循环任务完成历史、标签管理。
6. **日志**：查看运行日志与错误日志（`logs\`），支持导出。
7. **关于**：版本与作者联系方式（邮箱 1559573443@qq.com，QQ 1559573443）。

## 六、目录结构

```
ZhaxxxxxxQAQ\
├── ZhaxxxxxxQAQ.vbs        静默启动器（开机自启指向它）
├── 启动 ZhaxxxxxxQAQ.bat    调试用启动器
├── 使用说明.md
├── config.json             全部设置（主题/快捷键/窗口位置等）
├── app\                    程序源码（main.py 等）
├── venv\                   内置 Python 运行环境（无需系统安装依赖）
├── resources\              图标与作者 logo
├── data\data.json          全部事项数据
├── logs\                   运行/错误日志
├── 导出\                   导出的 CSV/JSON/主题/日志
└── 工作文件夹\             事项绑定的默认工作目录
```

- 卸载：托盘 → 退出程序后，直接删除本文件夹即可（注册表 Run 项名为 ZhaxxxxxxQAQ，可自行清理）。
- 迁移：整个文件夹复制到新位置后运行 vbs 即可，数据不丢失。

## 七、提醒说明

- 待办：到达“截止时间 − 提前量”时弹窗 + 托盘气泡 + 主界面高亮。
- 循环任务：按周期与设定时间提醒当期实例。
- 提醒弹窗支持：✔完成 / 稍后 5 分钟、30 分钟、1 小时 / 自定义分钟数 / 关闭。
